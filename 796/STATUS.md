# Status: Erdős #796

## Goal

Derive the first structural decomposition that isolates the second-order contribution to $g_3(n)$.

## Context

$g_3(n)$ maximizes $|A|\subseteq[1,n]$ subject to every integer having at most two representations $m=a_1a_2$ with distinct ordered-by-size factors $a_1<a_2$ from $A$.

## Constraints

No web search or external solution lookup. Representation counts require $a_1<a_2$.

## Deliverables

An extremal reformulation, small-case computation, and at least four distinct upper/lower-bound routes.

## Done when

A sharp construction or upper-bound lemma improves the initial state and is proved or reproducibly computed.

## State

Unstarted. Main claim open locally.
