# Status: Erdős #878

## Goal

Decompose the problem into independently solvable extremal, almost-all, equality-counting, and summatory subproblems.

## Context

$f(n)$ sums $p_i^{\ell_i}$ over the distinct prime support of $n$, with $p_i^{\ell_i}\leq n<p_i^{\ell_i+1}$. In $F(n)$, the count of chosen pairwise-coprime $a_i$ is a separate index from the factorization exponents $k_i$.

## Constraints

No web search. Resolve the optimization defining $F(n)$ exactly before using approximations.

## Deliverables

An exact fixed-support optimization, numerical experiments, and separate route ledgers for every subquestion.

## Done when

At least one subquestion is solved completely or reduced to a precise standard theorem plus a proved local lemma.

## State

Unstarted. Main claims open locally.
